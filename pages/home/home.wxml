<block wx:if="{{isAuth}}">
  <nav-bar title="首页" showBack="{{false}}">
    <view class="">123</view>
  </nav-bar>

  <page-container refreshable isRefreshing="{{isRefreshing}}" bind:refresh="onPullDownRefresh"
    bind:loadMore="onReachBottom">
    <view class="container">
      <!-- 仓库列表 -->
      <view class="repo-list">
        <view wx:for="{{repos}}" wx:key="id" class="repo-item" bindtap="goToRepo" data-repo="{{item}}">
          <text class="repo-name">{{item.full_name}}</text>
          <text class="repo-desc">{{item.description || '-'}}</text>
          <view class="repo-stats">
            <view class="stat-item" wx:if="{{item.language}}">
              <text class="stat-text">{{item.language}}</text>
            </view>
            <view class="stat-item">
              <text class="stat-icon">⭐</text>
              <text class="stat-text">{{item.stargazers_count}}</text>
            </view>
            <view class="stat-item">
              <text class="stat-icon">📅</text>
              <text class="stat-text">{{item.updated_at}}</text>
            </view>
          </view>
        </view>

        <!-- 加载更多 -->
        <view class="load-more" wx:if="{{hasMore}}">
          <text class="load-text">加载中...</text>
        </view>
        <view class="load-more" wx:else>
          <text class="load-text">没有更多了</text>
        </view>
      </view>
    </view>
  </page-container>
</block>
<view wx:else class="auth-tip">
  <text class="tip-text">请先授权登录</text>
  <button class="btn-auth" bindtap="goToAuth">去授权</button>
</view>